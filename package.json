{
  "name": "cyclops",
  "private": true,
  "description": "Behavior-proof PR review — understanding-first, dual stores, generative proof UI",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "cli": "tsx packages/cli/src/main.ts",
    "workspace": "pnpm --filter @cyclops/workspace dev"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
