{
  "name": "verit",
  "private": true,
  "description": "Behavior-proof PR review: understanding-first, dual stores, generative proof UI",
  "license": "AGPL-3.0-only",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:integration": "VERIT_S3_TEST_ENDPOINT=http://localhost:9000 VERIT_S3_TEST_BUCKET=verit-proofs VERIT_S3_TEST_ACCESS_KEY_ID=verit VERIT_S3_TEST_SECRET_ACCESS_KEY=verit-dev-secret VERIT_NEO4J_URI=bolt://localhost:7687 VERIT_NEO4J_PASSWORD=verit-dev VERIT_PG_TEST_URL=postgres://verit:verit-dev@localhost:5433/verit pnpm --filter @verit/adapter-s3-blob --filter @verit/adapter-neo4j --filter @verit/dashboard test",
    "typecheck": "pnpm -r run typecheck",
    "cli": "tsx packages/cli/src/main.ts",
    "workspace": "pnpm --filter @verit/workspace dev"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
